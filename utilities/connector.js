import { connect } from "react-redux";
import mapStateToProps from 'react-redux/lib/connect/mapStateToProps';

export const connector = (stateMapper, dispatchMapper, view) => {
    return connect(

    )
};
